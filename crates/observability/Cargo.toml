[package]
name = "observability"
version = "0.1.0"
edition = "2021"

[dependencies]
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.20", features = ["fmt", "std"] }

[lib]
path = "src/lib.rs"
