[package]
name = "app-live"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
observability = { path = "../observability" }
state = { path = "../state" }

[dev-dependencies]
chrono = "0.4"
domain = { path = "../domain" }
rust_decimal = "1.40.0"
state = { path = "../state" }

[lib]
path = "src/lib.rs"
