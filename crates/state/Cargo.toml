[package]
name = "state"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.44", default-features = false, features = ["clock", "std"] }
domain = { path = "../domain" }
rust_decimal = "1.40.0"

[lib]
path = "src/lib.rs"
