[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.42", default-features = false, features = ["clock", "std"] }
rust_decimal = "1.39.0"

[lib]
path = "src/lib.rs"
